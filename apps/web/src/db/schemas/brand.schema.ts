// ─── Brand DocType ────────────────────────────────────────────────────────────

export type BrandDocType = {
    id: string;
    storeId: string;
    name: string;
    createdAt?: string;
    updatedAt?: string;
};
